Request.txt
the first line of request.txt is the number of proccessors. After first line, the rest of the rows represent the number of items in each processor. in these rows the number before the colon represent the number of items in each row.

Objective.c
allows you to choose the size of timestamp matrix
